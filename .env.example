# Copy this to .env and fill in real values before running.
# SQLite for now — a single file at data/app.db, no server/Docker needed.
# To switch to Postgres later: change provider in prisma/schema.prisma to
# "postgresql" and point this at a real Postgres connection string instead.
DATABASE_URL="file:../data/app.db"
SUPER_ADMIN_EMAIL=kartik@contentninja.in
SUPER_ADMIN_PASSWORD=2effce3
PROMPTWATCH_BASE_URL=https://server.promptwatch.com/api/v2
